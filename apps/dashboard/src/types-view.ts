// Which top-level surface is showing. "chats" is the default (the roster +
// conversation). The others are the surfaces we add on top of Grok Bot's model.
export type View = "chats" | "skills" | "routines" | "audit" | "settings";
